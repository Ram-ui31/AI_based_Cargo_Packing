__pycache__/
*.pyc
.DS_Store
backend/uploads/*.csv

# desktop app build artifacts (large, regenerated by desktop/build.sh) --
# never commit these, only the source (launcher.py, build.sh)
desktop/build/
desktop/dist/
desktop/.buildenv/
desktop/*.spec
